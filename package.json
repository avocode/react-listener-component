{
  "name": "react-listener-component",
  "version": "2.0.0",
  "main": "lib/listener-component",
  "scripts": {
    "build": "rm -rf lib && coffee -c -o lib src && cp src/*.js lib/",
    "prepublish": "npm run build",
    "test": "npm run build && ava lib/test/**/*.spec.js"
  },
  "peerDependencies": {
    "react": ">=0.14.3"
  },
  "devDependencies": {
    "ava": "0.16.0",
    "coffee-script": "1.9.1",
    "react": "0.14.3",
    "react-addons-test-utils": "0.14.3",
    "react-test-render": "1.1.1"
  }
}
